import { ChainInfo, SupportedChainId } from "../types";
export declare const CHAIN_INFO: {
    [chainId in SupportedChainId]: ChainInfo;
};
export declare const isSupportedChain: (chainId: number) => chainId is SupportedChainId;
